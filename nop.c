#include "monty.h"

/**
* _nop - literally doesn't do anything XD
* @stack: what's the point?
* @line_number: what's the point?
*/
void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
